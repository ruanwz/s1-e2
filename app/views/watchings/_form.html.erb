<%= form_for(@watching) do |f| %>
  <% if @watching.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@watching.errors.count, "error") %> prohibited this watching from being saved:</h2>

      <ul>
      <% @watching.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :repository_id %><br />
    <%= f.text_field :repository_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
